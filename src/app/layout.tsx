import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Shaoming Wu - Robotics & Fullstack Developer",
  description: "Personal portfolio of Shaoming Wu - Robotics leader, fullstack developer, and STEM education advocate. Expertise in JavaScript, React, Python, C++, and more.",
  keywords: ["Shaoming Wu", "robotics", "fullstack developer", "JavaScript", "React", "Python", "C++", "portfolio"],
  authors: [{ name: "Shaoming Wu" }],
};

export const viewport = {
  width: "device-width",
  initialScale: 1,
  themeColor: "#0a0a0a",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark scroll-smooth">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased overflow-x-hidden`}
      >
        {children}
      </body>
    </html>
  );
}
